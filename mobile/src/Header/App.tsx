import React from 'react-native';
import { Container, Top, Name } from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <Top />
      <Name />
    </Container>
  );
};

export default Header;
